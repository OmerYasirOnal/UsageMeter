# SwiftPM
.build/
.swiftpm/
Package.resolved

# Assembled app bundle (build artifact)
UsageMeter.app/

# macOS
.DS_Store

# Local tooling / internal build brief (not part of the product)
.claude/
usage-meter-prompt.md

# Xcode — the project is generated from project.yml via `make xcodeproj`
*.xcodeproj
*.xcworkspace/xcuserdata/
DerivedData/

# Release artifacts
UsageMeter-macOS.zip
*.zip

# Xcode build output
build/

# stray release/screenshot artifacts
UsageMeter-macOS.zip
pricing_page.png
